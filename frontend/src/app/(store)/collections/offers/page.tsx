'use client';

/**
 * Offers — /collections/offers
 * 
 * Campaign-style redesign: Hero + Offer Types + Dynamic Grid + Countdown
 * ORA Design System — Urgency-driven, no placeholders, admin-controlled
 */

import OffersPageContent from '@/components/offers/OffersPageContent';

export default function OffersPage() {
  return <OffersPageContent />;
}
