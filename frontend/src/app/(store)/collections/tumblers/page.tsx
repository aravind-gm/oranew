'use client';

/**
 * Tumblers — /collections/tumblers
 * 
 * Full redesign: 3 Stanley tumblers at ₹1,099 / ₹2,099 / ₹3,099
 * Marketing-driven: Hero → Showcase × 3 → Why Stanley → Comparison → Reviews → FAQ → CTA
 */

import TumblersPageContent from '@/components/tumblers/TumblersPageContent';

export default function TumblersPage() {
  return <TumblersPageContent />;
}
